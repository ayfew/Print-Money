# Just enough to run `pm daily`. The full engine needs numpy and scipy for the
# linear program and the path simulator; the morning brief needs none of that,
# and a cloud runner that installs only these three is up in seconds.
httpx>=0.27
PyYAML>=6.0
rich>=13.7

# ...plus ccxt, since the cross-venue funding spread became part of the brief
# rather than a research aside. Without it the cloud run still succeeds and
# still publishes - it just quietly drops the one section that names something
# actionable, which is the worst way to lose a feature.
ccxt>=4.5.0
