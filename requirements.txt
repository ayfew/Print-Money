# core - everything the engine needs to scan, model, solve and paper trade
numpy>=1.26
scipy>=1.11
httpx>=0.27
PyYAML>=6.0
rich>=13.7

# live trading only. not needed for paper mode; install deliberately.
# py-clob-client>=0.17

# The research extras live in requirements-research.txt. They were listed here
# with an `extra == "research"` marker, which is only defined while resolving a
# package's extras - in a bare requirements file it evaluates False, so pip
# skipped both lines without a word and nobody got the tools the comment
# promised. A separate file cannot lie the same way.
