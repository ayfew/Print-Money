# core - everything the engine needs to scan, model, solve and paper trade
numpy>=1.26
scipy>=1.11
httpx>=0.27
PyYAML>=6.0
rich>=13.7

# live trading only. not needed for paper mode; install deliberately.
# py-clob-client>=0.17

# Optional, and each earns its place for exactly one job:
#   TA-Lib  the 161-indicator sweep in `pm indicators`. Since 0.6.5 it ships
#           binary wheels, so the old compile-the-C-library step is gone.
#   ccxt    one interface over 100+ exchanges for `pm venues`. Taken on for the
#           funding endpoint only - it is not a strategy library, so it imports
#           nobody's opinion about what to trade.
TA-Lib>=0.7.1 ; extra == "research"
ccxt>=4.5.0 ; extra == "research"
